<Page x:Class="ChestOrMonster.WPF.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ChestOrMonster.WPF.Pages"
      mc:Ignorable="d"
      Title="MainPage" MinHeight="450" MinWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StepTextBlock" Grid.Column="0" Text="Этаж 1" FontSize="20" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <ProgressBar x:Name="PlayerHPBar" Grid.Column="1" Minimum="0" Maximum="100" Value="100"/>
            <TextBlock x:Name="PlayerHPTextBlock" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14" Foreground="White" FontStyle="Italic" FontWeight="Bold"/>
        </Grid>
        
        <ContentControl x:Name="MainControl" Grid.Row="1"/>
        
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel Margin="10" VerticalAlignment="Center">
                    <Image Source="../Images/WeaponIcon.png" Width="100"/>
                    <TextBlock x:Name="WeaponTextBlock" Text="Название Характеристика" TextWrapping="Wrap" FontSize="16"/>
                </StackPanel>
                <StackPanel Margin="10" VerticalAlignment="Center">
                    <Image Source="../Images/ArmorIcon.png" Width="100"/>
                    <TextBlock x:Name="ArmorTextBlock" Text="Название Характеристика" TextWrapping="Wrap" FontSize="16"/>
                </StackPanel>
            </StackPanel>
            <ScrollViewer x:Name="LogScroller" Grid.Column="1">
                <ItemsControl ItemsSource="{Binding Logger}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontSize="16"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
        
    </Grid>
</Page>
